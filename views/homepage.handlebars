
<main class="'container container-fluid mt-5">
    <div class="stutus" id="stutus-bar">

        
    </div>
   
   <div>
        <h1>Search for a book</h1>
        <input class="book-input" type="text" placeholder="Search for a book" value="Harry Potter">
        <button class="search-btn">Search</button>
    </div>

    <div class="current-search" id="book-search-results">
        
    </div>
</main>

<!--{{#each reviews as |review|}}
<div class="row mb-4 post">
    <div class="">
        <h2>
            <a href="/review/{{isbn}}">{{review.title}}</a>
        </h2>
        <p>Created by {{review.user.name}} on {{createdAt}}</p>
    </div>
    <div class="">
        <p>
            {{review.review}}
        </p>
    </div>
</div>
{{/each}}-->

<script src="js/api.js"></script>